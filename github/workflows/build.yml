name: Build

on:
  push:
    branches: [main]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install -g @neutralinojs/neu
      - run: neu build
      - uses: actions/upload-artifact@v4
        with:
          name: linux
          path: dist/typing-learner-linux

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install -g @neutralinojs/neu
      - run: neu build
      - uses: actions/upload-artifact@v4
        with:
          name: windows
          path: dist/typing-learner-win.exe
